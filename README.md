A quick test suite for the Drift chatbot  
  
## Install  
First, install [NodeJS](https://nodejs.org/en/)  
  
Then, install the dependencies    
`npm install`  

After that you can run the tests with the command:  
`nightwatch tests`
